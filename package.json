{
  "name": "project-sunrise-launcher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "build:app": "tauri build --no-bundle",
  "package:deck": "bash deck/deck-build.sh",
  "build:deck": "npm run build && npm run build:app && npm run package:deck",
  "preview": "vite preview",
  "tauri": "tauri"
},
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
